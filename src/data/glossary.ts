export interface GlossaryItem {
  term: string
  cat: string
  def: string
  ex?: string
}

export const GLOSSARY_ITEMS: GlossaryItem[] = [
  {term:"Case",cat:"BenSelect Core",def:"The top-level enrollment group representing one employer's enrollment site. Also called Group. All plan years, employees, and benefit configurations live under the Case.",ex:"Event.Case.EnabledPlanYears[0]"},
  {term:"Plan",cat:"BenSelect Core",def:"A benefit category umbrella (e.g. 'Voluntary Life', 'Medical', 'STD'). Plans organize products but employees never enroll in plans directly — they enroll in Products.",ex:"// Plans are waived at the plan level, not scripted directly"},
  {term:"Product",cat:"BenSelect Core",def:"The specific carrier policy within a Plan (e.g. 'Colonial Life Term', 'Cigna PPO High'). Products have rates, eligibility rules, GI limits, and benefit levels. This is what employees actually enroll in.",ex:"Event.ProductTag  // XML tag name of current product"},
  {term:"Application",cat:"BenSelect Core",def:"An in-progress enrollment. Created when an employee begins enrolling in a product. Becomes a Coverage only when all required forms including the final confirmation are signed.",ex:"Event.Application.EffectiveDate  // override in OnEligible"},
  {term:"Coverage",cat:"BenSelect Core",def:"A confirmed enrollment — an Application after all required forms are signed. Coverage amounts at or below GI are immediately Active. Amounts above GI are Pending awaiting carrier review.",ex:"Event.LastCoverage  // prior year confirmed coverage"},
  {term:"Rate Group",cat:"BenSelect Core",def:"A named set of rates configured in BenSelect case setup. Scripts assign employees to rate groups to route them to the correct rate table (e.g. 'Prior Decline', 'Smoker', 'Employee', 'Family').",ex:"Event.Engine.RateGroup = \"Prior Decline\""},
  {term:"Plan Year",cat:"BenSelect Core",def:"The 12-month benefit coverage period. BenSelect stores all plan years in EnabledPlanYears[]. Event.PlanYear is a shortcut to the current plan year — but is null in the first plan year of a case build.",ex:"Event.Case.EnabledPlanYears[0].PlanYearStartDate"},
  {term:"Open Enrollment (OE)",cat:"BenSelect Core",def:"The annual period when all eligible employees can make benefit elections. Scripts use enrollment start/end dates from the plan year to differentiate OE logic from new hire logic."},
  {term:"Payer Product",cat:"BenSelect Core",def:"The carrier-specific product configuration containing benefit levels, age bands, GI limits, and rate tables. Accessed in scripts via Event.Config.GetPayerProductInfo(payerProductID).",ex:"Event.Config.GetPayerProductInfo(Event.Product.PayerProductID)"},
  {term:"XML Tag Name",cat:"BenSelect Core",def:"The identifier string assigned to a plan in BenSelect case setup, used as the key in Event.Applications[]. For example, 'MEDICAL' is the tag for the medical plan. Must match exactly (case-sensitive).",ex:"Event.Applications[\"MEDICAL\"]"},
  {term:"Benefit Level",cat:"BenSelect Core",def:"One of the coverage amounts available in a product. A voluntary life product might have levels: $50,000 / $100,000 / $200,000. Scripts must loop over all benefit levels when setting maximums.",ex:"payerProduct.BenefitLevels.Length  // number of levels"},
  {term:"Age Band",cat:"BenSelect Core",def:"A range of ages within a benefit level that may have different rates or step-down percentages. For example: under 30, 30-39, 40-49, 50-59, 60-64, 65+.",ex:"payerProduct.AgeBand[0].Length  // bands in first level"},
  {term:"Step-Down",cat:"BenSelect Core",def:"A percentage reduction of coverage that applies as employees age. GetStepDown(levelIndex, bandIndex) returns the percentage of original coverage retained. 0 from GetStepDown() means no step-down.",ex:"payerProdInfo.GetStepDown(0, ageBandIndex)"},
  {term:"CoverageTypeID",cat:"BenSelect Core",def:"A numeric code identifying the coverage tier: 4 = Employee Only (EO), 5 = Employee + Spouse, 6 = Employee + Children, 7 = Family.",ex:"Event.Applications[\"MEDICAL\"].CoverageTypeID == 4"},
  {term:"Waiting Period",cat:"Enrollment Timing",def:"The number of days/months/years after the eligibility date before coverage can become effective. Controlled by Event.Engine.Config.WaitingPeriod and WaitingPeriodType.",ex:"eligibilityDate.AddMonths(waitingPeriod) // WaitingPeriodType=1"},
  {term:"Grace Period",cat:"Enrollment Timing",def:"The window of time an employee has to complete their enrollment after becoming eligible. Controlled by Event.Config.GracePeriod and GracePeriodType. After this window, the employee is a Late Entrant.",ex:"eligibilityDate.AddDays(Event.Config.GracePeriod)"},
  {term:"Eligibility Date",cat:"Enrollment Timing",def:"The date an employee becomes eligible to begin enrollment. Usually DateOfHire unless a separate EligibilityDate is configured. Always null-check EligibilityDate before using it.",ex:"Event.Employee.EligibilityDate  // nullable — null-check first"},
  {term:"Effective Date",cat:"Enrollment Timing",def:"The date coverage begins. Calculated from eligibility date + waiting period + effective date calc type. Can be overridden with Event.Application.EffectiveDate in OnEligible scripts.",ex:"Event.Application.EffectiveDate = calculatedDate"},
  {term:"Cutoff Date",cat:"Enrollment Timing",def:"The last possible hire/eligibility date at which an employee could have enrolled and gotten an effective date within the current plan year. Employees eligible before the cutoff who didn't enroll are Late Entrants during OE.",ex:"currentPlanYearStartDate.AddMonths(-1).AddDays(1) // case 0"},
  {term:"Late Entrant",cat:"Enrollment Timing",def:"An employee who missed their initial new hire enrollment window (eligibilityDate < cutOffDate, or Now > eligibilityEndDate). Late Entrants receive a lower maximum benefit amount (LATE_ENTRANT_MAX_BEN_AMT).",ex:"if(Now > eligibilityEndDate && Event.LastCoverage == null)"},
  {term:"Deduct Before Effective Date",cat:"Enrollment Timing",def:"A BenSelect setting that causes deductions to begin before coverage is effective. Can be disabled in OnLifeEvent scripts: Event.Config.DeductionDateBeforeEffectiveDate = 0.",ex:"Event.Config.DeductionDateBeforeEffectiveDate = 0"},
  {term:"OnEligible",cat:"Script Event Types",def:"The event type that fires each time BenSelect evaluates a product for eligibility. Runs once per eligible product per enrollment session. Use for: rate groups, GI limits, max benefit amounts, and eligibility control.",ex:"// EventType: OnEligible"},
  {term:"OnLifeEvent",cat:"Script Event Types",def:"The event type that fires when a qualifying life event occurs (hire, termination, marriage, etc.). Use for: effective date overrides, termination date overrides, disabling deduct-before.",ex:"// EventType: OnLifeEvent"},
  {term:"OnReport",cat:"Script Event Types",def:"The event type for report scripts. Fires for each cell in the report output. Can read/write Event.Value, suppress rows with Event.SkipRecord, and persist values with Event.Globals.",ex:"// EventType: OnReport"},
  {term:"Guaranteed Issue (GI)",cat:"Underwriting",def:"The maximum coverage amount a carrier will auto-approve without medical underwriting questions. Amounts at or below GI are immediately Active. Amounts above GI go to Pending status awaiting carrier review.",ex:"Event.Config.SetGILimit(i, n, giAmount)"},
  {term:"Event object",cat:"JScript API",def:"The root object injected by BenSelect into every script at runtime. Contains all accessible data: Employee, Engine, Config, Application, Action, PlanYear, Case, Product, and report-specific properties.",ex:"Event.Employee.DateOfHire"},
  {term:"Event.Debug()",cat:"JScript API",def:"The only output tool available in BenSelect scripts. Writes to the BenSelect administrator debug window only — employees never see this output. Used for testing like console.log().",ex:"Event.Debug(\"value: \" + someVar)"},
  {term:"Event.Eligible",cat:"JScript API",def:"Set to false in an OnEligible script to remove a product from the employee's enrollment options. Defaults to true for products configured as eligible in BenSelect case setup.",ex:"Event.Eligible = false  // removes from employee options"},
  {term:"Event.Engine.RateGroup",cat:"JScript API",def:"Set this in an OnEligible script to assign the employee to a named rate group. The string must exactly match a rate group name configured in BenSelect case setup.",ex:"Event.Engine.RateGroup = \"Prior Decline\""},
  {term:"Event.Applications[]",cat:"JScript API",def:"Cross-plan application lookup by XML tag name. Returns the in-progress application for another plan if the employee has already visited it in this session. Always null-check before accessing properties.",ex:"Event.Applications[\"MEDICAL\"]?.CoverageTypeID"},
  {term:"Event.LastCoverage",cat:"JScript API",def:"The employee's prior year confirmed coverage for the current product. Null if the employee has never been enrolled in this product before. Check for null before accessing .BenefitAmount.",ex:"if(Event.LastCoverage != null){ setMaximum(Event.LastCoverage.BenefitAmount + INC_AMT); }"},
  {term:"Event.Config.SetMaxBenefitAmount()",cat:"JScript API",def:"Sets the maximum benefit amount for a specific benefit level and age band combination. Must be called for EVERY combination of i (benefit level) and n (age band) in a nested loop.",ex:"Event.Config.SetMaxBenefitAmount(i, n, trueMax)"},
  {term:"Event.Config.SetGILimit()",cat:"JScript API",def:"Sets the Guaranteed Issue limit for a specific benefit level and age band combination. Must be called for every combination in a nested loop, same pattern as SetMaxBenefitAmount.",ex:"Event.Config.SetGILimit(i, n, giAmount)"},
  {term:"Event.Action.TerminationDate",cat:"JScript API",def:"Set this in an OnLifeEvent script to override when coverage ends. Different from Event.Employee.TerminationDate which is the employment termination date (read-only input).",ex:"Event.Action.TerminationDate = covTermDate"},
  {term:"Event.Application.EffectiveDate",cat:"JScript API",def:"Set this in an OnEligible script to override when coverage begins. Used in effective date calculation scripts to apply the calculated effective date after waiting period and calc type logic.",ex:"Event.Application.EffectiveDate = effectiveDate"},
  {term:"Event.SkipRecord",cat:"JScript API",def:"Set to true in a report script to suppress the entire current row from the report output. Used to filter out dependent rows, zero-amount records, or rows that don't meet specific criteria.",ex:"Event.SkipRecord = true  // suppress this row"},
  {term:"Event.Globals",cat:"JScript API",def:"A key-value collection that persists across all rows during report execution. Used for running totals, tracking state across rows, or carrying forward calculated values.",ex:"Event.Globals[\"total\"] = Event.Globals[\"total\"] + Event.Value"},
  {term:"System.DateTime.Today",cat:".NET Classes",def:"The .NET static property for today's date. Returns a DateTime object with time set to midnight. NOT the same as JavaScript's new Date() — use this instead in BenSelect scripts.",ex:"var Now = System.DateTime.Today"},
  {term:"DateTime.Parse()",cat:".NET Classes",def:"Converts a string representation of a date into a System.DateTime object. Used after String.Format to normalize nullable BenSelect dates into DateTime objects that support arithmetic.",ex:"DateTime.Parse(String.Format('{0:MM/dd/yyyy}', someDate))"},
  {term:"String.Format()",cat:".NET Classes",def:".NET string formatting method. In BenSelect, primarily used to convert DateTime objects to MM/dd/yyyy strings before parsing: String.Format('{0:MM/dd/yyyy}', dateObject).",ex:"String.Format('{0:MM/dd/yyyy}', Event.Employee.EligibilityDate)"},
  {term:".AddDays(n)",cat:".NET Classes",def:"Returns a NEW DateTime that is n days after the original. Does NOT modify the original. Must assign the result: effectiveDate = eligibilityDate.AddDays(30).",ex:"effectiveDate = eligibilityDate.AddDays(waitingPeriod)"},
  {term:".AddMonths(n)",cat:".NET Classes",def:"Returns a NEW DateTime that is n months after the original. Correctly handles varying month lengths (e.g. Jan 31 + 1 month = Feb 28, not Mar 3).",ex:"effectiveDate = eligibilityDate.AddMonths(waitingPeriod)"},
  {term:"new DateTime(year, month, day)",cat:".NET Classes",def:"Constructs a DateTime for a specific date. Used to build the 1st of a month: new DateTime(d.Year, d.Month, 1). Foundation of month-end date construction.",ex:"new DateTime(termDate.Year, termDate.Month, 1).AddMonths(1).AddDays(-1)"},
  {term:".toString()",cat:".NET Classes",def:"Converts a .NET object to its string representation. Used for Event.EventType which is a .NET enum: var theEvent = Event.EventType.toString(); required before string comparison.",ex:"var theEvent = Event.EventType.toString()"},
  {term:".IndexOf(substring)",cat:".NET Classes",def:"Finds the position of a substring. Returns -1 if not found, or the 0-based position if found. Used with .ToUpper() for case-insensitive custom field string searches.",ex:"customField.ToUpper().IndexOf(\"2025:Y\") > -1"},
  {term:"CLR",cat:"JScript.NET Concepts",def:"Common Language Runtime — the .NET execution engine. All .NET languages compile to intermediate language and run on the CLR. Gives JScript.NET access to the .NET Framework Class Library including System.DateTime.",ex:"// JScript.NET runs on the CLR, not in a browser"},
  {term:"CTS",cat:"JScript.NET Concepts",def:"Common Type System — ensures all .NET languages share type definitions. A DateTime in JScript.NET is the same object as a DateTime in C#. Enables cross-language interop.",ex:"// DateTime from JScript.NET is identical to C# DateTime"},
  {term:"JIT Compilation",cat:"JScript.NET Concepts",def:"Just-In-Time compilation. JScript.NET scripts are compiled at runtime before execution, not interpreted line by line. A syntax error anywhere in the script crashes the entire script, not just one line.",ex:"// Syntax errors crash the whole script — no partial execution"},
  {term:"const DEBUG = false",cat:"JScript.NET Concepts",def:"The professional debug flag pattern used in production BenSelect scripts. Wraps all Event.Debug() calls in if(DEBUG) blocks. Change to true for testing, back to false for production — one line controls all debug output.",ex:"const DEBUG = false; if(DEBUG){ Event.Debug(\"var: \" + x); }"},
  {term:"CustomFields[]",cat:"JScript.NET Concepts",def:"A hashtable on the Employee object containing raw custom field values. Boolean custom fields return 1 or 0 (not true/false). Use GetCustom() for a null-safe accessor. Always compare boolean fields to == 1.",ex:"var flag = Event.Employee.CustomFields[\"My Flag\"];\nif(flag == 1){ ... }"},
  {term:"GetCustom()",cat:"JScript.NET Concepts",def:"Null-safe custom field accessor on the Employee object. Returns null if the field is empty or not configured on the case. Safer than CustomFields[] for text and DateTime custom fields.",ex:"Event.Employee.GetCustom(\"Coverage Termination Date\")"},
]

export const GLOSS_CATS = [...new Set(GLOSSARY_ITEMS.map(g => g.cat))]
