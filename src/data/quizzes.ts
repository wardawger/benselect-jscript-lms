export interface QuizQuestion {
  q: string
  opts: string[]
  a: number
  exp: string
}

export const QUIZ: Record<number, QuizQuestion[]> = {
1:[
{q:"What does CLR stand for in the .NET platform?",opts:["Common Language Runtime","Core Library Reference","Compiled Logic Runtime","Custom Language Repository"],a:0,exp:"CLR = Common Language Runtime. It manages execution, memory, and type safety for all .NET languages including JScript.NET running inside BenSelect."},
{q:"True or False: JScript.NET inside BenSelect can access document.getElementById().",opts:["True — it is just JavaScript","False — JScript.NET runs server-side with no browser DOM","True — but only in report scripts","False — use Event.Document instead"],a:1,exp:"JScript.NET runs server-side on the .NET CLR. There is no browser DOM, no window, no document object. All data access goes through the injected Event object."},
{q:"What is the primary object all BenSelect scripts interact with?",opts:["BenSelect","Event","Employee","Config"],a:1,exp:"The Event object is injected by BenSelect at runtime and is the root of all data access in every script type — OnEligible, OnLifeEvent, and Report."},
{q:"What does Event.Debug() do in BenSelect?",opts:["Throws an error to stop the script","Shows output to the enrolling employee","Writes to the BenSelect admin debug window for testing","Sends an email alert to the administrator"],a:2,exp:"Event.Debug() writes to the BenSelect debug window, visible only during administrator testing. Employees never see it — it is the equivalent of console.log() for BenSelect scripts."},
{q:"JScript.NET differs from browser JavaScript primarily because it runs on ___",opts:["Node.js","The browser V8 engine","The .NET Common Language Runtime","A custom Selerix interpreter"],a:2,exp:"JScript.NET compiles and runs on the .NET CLR, giving it access to the entire .NET Framework Class Library including System.DateTime."}
],
2:[
{q:"Why must you write if(field == 1) instead of if(field) for boolean custom fields?",opts:["Faster performance","Boolean custom fields output numeric 1 or 0, not true or false — a truthy check on 0 behaves unexpectedly","== 1 is required .NET syntax","BenSelect documents require strict equality everywhere"],a:1,exp:"Boolean custom fields in BenSelect output the numbers 1 and 0, not true/false. Always compare explicitly to 1."},
{q:"What is the difference between const and var in JScript.NET?",opts:["No difference — both work the same","const creates a function-scoped variable; var creates a block-scoped one","const declares an immutable value that cannot be reassigned; var can be reassigned","const is only for strings; var works for all types"],a:2,exp:"const declares a constant that cannot be reassigned — used for configuration values like CUTOFF_DAY = 15, DEATH_REASON_ID = 304. var is for values that change during script execution."},
{q:"In a switch for WaitingPeriodType, case 1 means Months. Which method adds months to a DateTime?",opts:[".AddMonths(n)",".PlusMonths(n)",".AddDays(n * 30)",".Months(n)"],a:0,exp:"The .NET DateTime method is .AddMonths(n). This is used throughout BenSelect date calculation scripts."},
{q:"What happens if you access Event.PlanYear.PlanYearStartDate when Event.PlanYear is null?",opts:["It returns null gracefully","It returns January 1st 1900 as a default","It throws a null reference exception and can crash the enrollment","It uses the case default plan year automatically"],a:2,exp:"Accessing a property on null throws a null reference exception. This is why every script using Event.PlanYear must check if(Event.PlanYear) first."},
{q:"What type does Event.Employee.DateOfHire return?",opts:["A JavaScript string","A JavaScript Date object","A System.DateTime .NET object","A .NET DateTimeOffset"],a:2,exp:"BenSelect date properties return System.DateTime objects, which have .NET methods like .AddDays(), .AddMonths(), and properties like .Day, .Month, .Year."},
{q:"Which is the correct null-safe pattern for EligibilityDate with a fallback to DateOfHire?",opts:["var d = Event.Employee.EligibilityDate || Event.Employee.DateOfHire","if(Event.Employee.EligibilityDate != null){ eligibilityDate = DateTime.Parse(String.Format('{0:MM/dd/yyyy}',Event.Employee.EligibilityDate)); } else { eligibilityDate = Event.Employee.DateOfHire; }","var d = Event.Employee.EligibilityDate ?? Event.Employee.DateOfHire","eligibilityDate = Event.Employee.EligibilityDate.GetValueOrDefault(Event.Employee.DateOfHire)"],a:1,exp:"Null-check EligibilityDate, then parse through DateTime.Parse(String.Format('{0:MM/dd/yyyy}',...)) to get a reliable DateTime."}
],
3:[
{q:"What keyword returns a value from a JScript.NET function?",opts:["output","send","return","yield"],a:2,exp:"return sends a value back from a function — same syntax as standard JavaScript."},
{q:"How do you return multiple values from a single JScript.NET function?",opts:["Return an object literal with named properties","Return new Array(val1, val2) and index with [0] and [1]","Use out parameters like C#","Return a comma-separated string and split it"],a:1,exp:"The BenSelect pattern is: return new Array(eligibilityDate, eligibilityEndDate); then consume as var range = GetRange(); var start = range[0]; var end = range[1];"},
{q:"Why is getCurrentPlanYearStartDate() extracted as a separate function?",opts:["JScript.NET requires all logic inside functions","To avoid repeating the null-check logic every time the start date is needed","API calls require function wrappers","Constants cannot be used outside function scope"],a:1,exp:"Extracting reusable logic into named functions prevents repetition and makes scripts maintainable."},
{q:"True or False: Functions in JScript.NET must declare a return type.",opts:["True — all .NET functions require type declarations","False — return types are optional in JScript.NET"],a:1,exp:"Unlike C# or VB.NET, JScript.NET does not require return type declarations."},
{q:"What is the key difference between a getter function and a setter function in BenSelect scripting patterns?",opts:["Setters read data; getters write data","Getter functions calculate and return a value; setter functions perform an action on the BenSelect object model without needing to return","There is no pattern difference","Getters use Event.Get(); setters use Event.Set()"],a:1,exp:"Getter functions (like getCurrentPlanYearStartDate) calculate and return a value. Setter functions (like setMaximum) perform an action on the Event object and typically return nothing."}
],
4:[
{q:"What is the difference between a property and a method on a BenSelect object?",opts:["Properties are always strings; methods are always numeric","A property reads or stores a value via dot notation; a method performs an action and may accept parameters","Properties are read-only; methods are write-only","There is no practical difference in JScript.NET"],a:1,exp:"Properties like Event.Employee.DateOfHire read or write a stored value. Methods like DateTime.Parse() perform an action."},
{q:"How do you get today's date in a BenSelect script?",opts:["new Date()","Date.now()","System.DateTime.Today","Event.Today"],a:2,exp:"System.DateTime.Today is the .NET static property for today's date. It returns a DateTime object."},
{q:"How do you read the day-of-month from Event.Employee.TerminationDate?",opts:["Event.Employee.TerminationDate.getDate()","Event.Employee.TerminationDate.Day","Event.Employee.TerminationDate[2]","TerminationDate.DayOfMonth"],a:1,exp:"The .NET DateTime property for day of month is .Day (capital D). Different from JavaScript's .getDate()."},
{q:"What does eligibilityDate.AddMonths(1) return?",opts:["It modifies eligibilityDate in place and returns nothing","A new DateTime object that is exactly one month later — original is unchanged","The number of months since a reference date","A string representing the new month"],a:1,exp:".AddMonths() returns a NEW DateTime — it does not modify the original. You must assign: effectiveDate = eligibilityDate.AddMonths(1)."},
{q:"How do you construct a DateTime for the 1st day of a month from an existing date variable d?",opts:["new DateTime(d.Year, d.Month, 1)","DateTime.Create(d.Year, d.Month, 1)","d.SetDay(1)","new Date(d.Year, d.Month, 1)"],a:0,exp:"new DateTime(year, month, day) is the .NET DateTime constructor. Foundation of month-end date construction: new DateTime(d.Year, d.Month, 1).AddMonths(1).AddDays(-1)."}
],
5:[
{q:"Why use DateTime.Parse(String.Format('{0:MM/dd/yyyy}', date)) instead of using the date directly?",opts:["It is faster","The date from BenSelect is a nullable object that may not directly support arithmetic — parsing normalizes it","BenSelect always returns dates as strings","Parse converts dates to UTC timezone"],a:1,exp:"EligibilityDate and similar properties are nullable .NET objects. Running them through DateTime.Parse(String.Format('{0:MM/dd/yyyy}',...)) normalizes them into reliable DateTime objects."},
{q:"Which method checks if a custom field string contains the substring '2025:Y'?",opts:[".Contains('2025:Y')",".equals('2025:Y')",".ToUpper().IndexOf('2025:Y') > -1",".StartsWith('2025:Y')"],a:2,exp:"IndexOf() returns -1 if not found, or its position if found. Combined with .ToUpper() for case-insensitive matching."},
{q:"How do you convert Event.EventType to a string for comparison?",opts:["Event.EventType.toString()","String(Event.EventType)","Event.EventType.Value","(string)Event.EventType"],a:0,exp:"Event.EventType is a .NET enum. Use .toString() to convert: var theEvent = Event.EventType.toString(); Then: if(theEvent == 'Terminated')."},
{q:"How do you access the first plan year's start date from EnabledPlanYears?",opts:["Event.Case.EnabledPlanYears.first().PlanYearStartDate","Event.Case.EnabledPlanYears[0].PlanYearStartDate","Event.Case.EnabledPlanYears.get(0).PlanYearStartDate","Event.Case.PlanYears[0].StartDate"],a:1,exp:"BenSelect collections use zero-based array indexing: EnabledPlanYears[0] gives the first plan year."}
],
6:[
{q:"What is the purpose of the const DEBUG = false pattern?",opts:["It speeds up script execution","It allows debug output to be toggled on/off by changing one line without deleting any code","It is required by BenSelect for all production scripts","It creates a persistent debug log file on the server"],a:1,exp:"const DEBUG = false wraps all Event.Debug() calls in if(DEBUG) blocks. Set to true for testing, back to false for production."},
{q:"What causes a null reference error in BenSelect scripts?",opts:["Using var instead of const for configuration values","Accessing a property or calling a method on an object that is null","Having too many Event.Debug() calls active at once","Using == instead of === for comparisons"],a:1,exp:"Null reference errors occur when you access a property or method on null. Always check if(Event.PlanYear) before accessing its properties."},
{q:"True or False: Event.Debug() output is visible to employees during their enrollment session.",opts:["True — it appears as a yellow tooltip","False — it only appears in the BenSelect administrator debug console"],a:1,exp:"Event.Debug() output is only visible in the BenSelect admin debug window — never to employees."},
{q:"What single change activates all debug output in the DEBUG flag pattern?",opts:["Remove all the if(DEBUG) wrapper checks","Change const DEBUG = false to const DEBUG = true at the top","Add Event.Debug('start') at the very top","Uncomment every debug line individually"],a:1,exp:"Changing one line — const DEBUG = false to const DEBUG = true — activates every debug block in the script."}
],
7:[
{q:"What is the difference between a Plan and a Product in BenSelect?",opts:["They are the same thing with different names","A Plan is a benefit category umbrella (e.g. Voluntary Life); a Product is the specific carrier policy within that plan","A Plan is what employees enroll in; a Product is just a configuration label","Plans are for medical benefits only; Products cover all other benefit types"],a:1,exp:"A Plan is the umbrella category. A Product is the specific carrier policy within that plan. Employees enroll in Products, not Plans."},
{q:"At what point does an Application become a Coverage in BenSelect?",opts:["When the employee clicks Submit","When the case administrator approves it","When all required forms — including the final confirmation — are signed","When the first premium deduction is processed"],a:2,exp:"An Application becomes a Coverage when all required forms including the final confirmation form are signed."},
{q:"What is a Late Entrant in BenSelect terminology?",opts:["An employee who enrolls on the very last day of open enrollment","An employee who missed their initial new hire enrollment window","An employee who was hired after the plan year began","An employee who changes plan elections mid-year"],a:1,exp:"A Late Entrant missed their new hire eligibility window. BenSelect scripts detect this and apply stricter GI limits."},
{q:"Why must you null-check Event.PlanYear before using it?",opts:["BenSelect deletes Event.PlanYear after open enrollment ends","Event.PlanYear is null during the very first plan year of a newly built case","It conflicts with EnabledPlanYears","Selerix removed this property in a recent update"],a:1,exp:"Event.PlanYear is null during the first plan year of a Selerix case build. Always check if(Event.PlanYear) and fall back to EnabledPlanYears[0]."},
{q:"What is Guaranteed Issue (GI) in BenSelect?",opts:["A government mandate requiring all applicants be accepted","The maximum coverage amount a carrier will approve without requiring medical underwriting","The minimum benefit amount required by law","A special rate category for government employees"],a:1,exp:"GI is the maximum coverage amount auto-approved without medical questions. Amounts above GI go to pending state."},
{q:"What is the difference between Waiting Period and Grace Period?",opts:["They are the same concept","Waiting Period = time after eligibility before coverage starts; Grace Period = the window to complete enrollment","Grace Period is company-paid; Waiting Period is employee-paid","Waiting Period is medical only; Grace Period is voluntary only"],a:1,exp:"Waiting Period: time before coverage starts. Grace Period: the window the employee has to complete enrollment."},
{q:"What does CoverageTypeID 4 represent in Selerix?",opts:["Employee + Spouse","Family (all dependents)","Employee Only","Employee + Children"],a:2,exp:"CoverageTypeID 4 = Employee Only tier in Selerix."},
{q:"What does the Selerix DAL provide to JScript?",opts:["A REST API for external integrations","Encapsulation of the Selerix Data Model — JScript accesses it through the Event object","A visual UI framework","An automated backup mechanism"],a:1,exp:"The DAL encapsulates the Selerix Data Model. JScript accesses the DAL through the Event object injected at runtime."}
],
8:[
{q:"When does OnEligible fire compared to OnLifeEvent?",opts:["OnEligible fires once per session; OnLifeEvent fires every 24 hours","OnEligible fires each time the engine evaluates a product for eligibility; OnLifeEvent fires when a qualifying life event occurs","OnEligible fires only for new hires; OnLifeEvent only for terminations","They fire at the same moment"],a:1,exp:"OnEligible runs every time BenSelect checks if a product should be offered. OnLifeEvent runs on hire/termination/life events."},
{q:"What is the difference between CustomFields['Name'] and GetCustom('Name')?",opts:["No difference","CustomFields returns the raw value; GetCustom returns null if the field is empty or not found — safer","GetCustom performs faster lookups","CustomFields only works for boolean fields"],a:1,exp:"GetCustom('Name') is the null-safe accessor — returns null if the field doesn't exist. Use GetCustom for text and DateTime custom fields."},
{q:"How do you mark a product as ineligible for an employee?",opts:["Event.Eligible = false","Event.Product.Eligible = false","Event.Engine.Eligible = false","Event.Config.SetEligible(false)"],a:0,exp:"Setting Event.Eligible = false removes that product from the employee's enrollment options."},
{q:"What does Event.Engine.RateGroup = 'Prior Decline' do?",opts:["Creates a new rate group","Assigns the employee to that named rate group, changing which rate table they use","Marks the application as declined","Disables the product permanently"],a:1,exp:"Event.Engine.RateGroup assigns the employee to a named rate group already configured in BenSelect case setup."},
{q:"How do you look up the medical plan's coverage tier while scripting the HSA?",opts:["Event.Medical.CoverageTypeID","Event.Employee.MedicalTier","Event.Applications['MEDICAL'].CoverageTypeID","Event.Config.GetMedicalTier()"],a:2,exp:"Event.Applications['MEDICAL'] accesses the in-progress medical application. Always null-check first."},
{q:"What does Event.LastCoverage represent and when is it null?",opts:["Most recent enrollment session; null if never logged in","Employee's prior year coverage for this product; null if never enrolled in this product","Last plan the employee waived; null if always enrolled","Employer's last census submission; null if pending"],a:1,exp:"Event.LastCoverage is the employee's prior coverage for this specific product. It is null when the employee has never been enrolled."},
{q:"In a report script, how do you change the output value of the current column?",opts:["Event.Column.Value = newValue","Event.Record.Override = newValue","Event.Value = newValue","Event.Output = newValue"],a:2,exp:"Event.Value holds the current column's output value. Assigning to it changes what gets written to that column."},
{q:"How do you suppress an entire row from a report?",opts:["Event.Row.Hide = true","Event.SkipRecord = true","return false","Event.Value = null on every column"],a:1,exp:"Event.SkipRecord = true causes the report engine to suppress the entire row."},
{q:"What is Event.Globals used for in report scripts?",opts:["Storing global configuration constants","Persisting values across rows — e.g. running totals","Accessing company-level configuration","Caching employee data"],a:1,exp:"Event.Globals is a key-value collection that persists between row processing calls."},
{q:"Which property sets the coverage end date override in an OnLifeEvent script?",opts:["Event.Application.TerminationDate","Event.Action.TerminationDate","Event.Employee.CoverageEndDate","Event.Config.TermDate"],a:1,exp:"Event.Action.TerminationDate is set in OnLifeEvent to override coverage end. Event.Employee.TerminationDate is read-only."}
],
9:[
{q:"Which property assigns an employee to a specific rate group?",opts:["Event.RateGroup","Event.Employee.RateGroup","Event.Engine.RateGroup","Event.Config.RateGroup"],a:2,exp:"Event.Engine.RateGroup assigns the employee to a named rate group."},
{q:"Why must all wellness payer products be marked eligible in config before the Wellness script runs?",opts:["BenSelect requires it for performance","OnEligible only fires for products already eligible in config — ineligible products are never evaluated","It ensures rate calculations complete first","Scripts cannot read custom fields unless all products are eligible"],a:1,exp:"OnEligible fires once per eligible product. The Wellness script checks Event.ProductTag for each and eliminates all except the correct match."},
{q:"What is Event.ProductTag?",opts:["The product's display name","The XML tag name of the product currently being evaluated in OnEligible","The product's internal numeric ID","The rate group currently assigned to this product"],a:1,exp:"Event.ProductTag is the XML tag name (e.g. 'Cigna_PPO_P_D_NW') assigned to the payer product in case setup."},
{q:"True or False: You can set Event.Eligible = true to force eligibility for a product that was set ineligible in case configuration.",opts:["True — scripts can override all configuration","False — scripts can only set Eligible to false; true is the default for configured-eligible products"],a:1,exp:"Scripts can set Eligible to false but cannot make a non-configured product eligible."},
{q:"What does Event.Applications['MEDICAL'] return when the employee hasn't visited the medical page?",opts:["An empty application object","The employee's prior medical coverage","null — the application does not yet exist","An error is thrown"],a:2,exp:"Event.Applications['MEDICAL'] returns null if no in-progress application exists for that plan tag."},
{q:"In the HSA Tier Based Rate Group script, what value of CoverageTypeID indicates Employee Only?",opts:["1","2","4","5"],a:2,exp:"CoverageTypeID 4 = Employee Only in Selerix."}
],
10:[
{q:"WaitingPeriodType = 0 maps to which unit of time?",opts:["Business days","Calendar days","Months","Years"],a:1,exp:"WaitingPeriodType: 0 = Days (.AddDays), 1 = Months (.AddMonths), 2 = Years (.AddYears)."},
{q:"How do you calculate the last day of the current month from termDate?",opts:["new DateTime(termDate.Year, termDate.Month, 31)","termDate.EndOfMonth()","new DateTime(termDate.Year, termDate.Month, 1).AddMonths(1).AddDays(-1)","termDate.AddDays(30)"],a:2,exp:"Go to 1st of current month → add 1 month → subtract 1 day. Handles all month lengths correctly."},
{q:"When should you use Event.Application.EffectiveDate vs Event.Action.TerminationDate?",opts:["Interchangeable","Event.Application.EffectiveDate overrides coverage start in OnEligible; Event.Action.TerminationDate overrides coverage end in OnLifeEvent","EffectiveDate for medical; TerminationDate for life","Both set in same event type"],a:1,exp:"Different purposes at different event types. EffectiveDate controls when coverage begins. TerminationDate controls when coverage ends."},
{q:"What is the cutoff date used for in BenSelect scripts?",opts:["Last day to submit an insurance claim","Last possible hire/eligibility date for enrollment in the current plan year — identifies late entrants during OE","Deadline for employers to upload census","Last day of the employee's grace period"],a:1,exp:"The cutoff date is the last possible hire/eligibility date at which an employee could enroll and get an effective date within the current plan year."},
{q:"EffectiveDateCalcType = 0 means what?",opts:["Immediately after the waiting period ends","First day of the month on or after the waiting period ends","First day of the next plan year","The exact day the waiting period ends"],a:1,exp:"EffectiveDateCalcType 0 = 1st of the month on or after. Type 1 = Immediately after (add 1 day)."},
{q:"Which Event property provides the configured grace period length?",opts:["Event.Engine.Config.GracePeriod","Event.Config.GracePeriod","Event.Employee.GracePeriod","Event.PlanYear.GracePeriod"],a:1,exp:"Event.Config.GracePeriod provides the grace period length. Event.Config.GracePeriodType provides the unit."},
{q:"Why do scripts check if(Now <= eligibilityEndDate) before applying new hire logic?",opts:["To limit script execution time","To determine whether the employee is still within their new hire enrollment window","To verify open enrollment has not ended","To confirm the plan year has started"],a:1,exp:"Now <= eligibilityEndDate confirms the employee is still in their new hire eligibility period. Past this date, late entrant logic applies."},
{q:"What does Part 2 of the Deduct Before Effective Date Override script do?",opts:["Calculates and sets the correct effective date","Disables the deduct-before functionality for life event enrollments by setting Event.Config.DeductionDateBeforeEffectiveDate = 0","Overrides the termination date","Sets the rate group for life event enrollments"],a:1,exp:"Part 2 (OnLifeEvent) disables deduct-before: Event.Config.DeductionDateBeforeEffectiveDate = 0."}
],
11:[
{q:"Why must you loop over both benefit levels AND age bands when calling SetMaxBenefitAmount?",opts:["BenSelect performance guidelines require it","Each combination has its own maximum — you must set all combinations or some use the wrong maximum","Age bands automatically update benefit levels","Benefit levels are ignored unless all age bands are set first"],a:1,exp:"BenSelect stores benefit maximums in a 2D structure [benefit level][age band]. SetMaxBenefitAmount(i, n, amount) sets one specific combination."},
{q:"What does Event.LastCoverage == null indicate in a benefit adjudication script?",opts:["The employee waived coverage","The employee has no prior coverage record — first-time electing or late entrant","The prior coverage was cancelled","The prior plan year has not been loaded"],a:1,exp:"null means the employee has no prior coverage record for this specific product."},
{q:"What is Event.Config.RoundFactor used for?",opts:["It caps the absolute maximum benefit amount","It rounds all benefit amounts to the nearest specified increment","It controls decimal places in display","It sets the minimum allowed benefit amount"],a:1,exp:"RoundFactor sets the rounding increment. A value of 100 rounds all benefit amounts to the nearest $100."},
{q:"What is the functional difference between SetMaxBenefitAmount() and SetGILimit()?",opts:["They are the same function","SetMaxBenefitAmount caps the maximum elected; SetGILimit sets the GI threshold below which no underwriting is required","SetGILimit is only for STD products","SetMaxBenefitAmount requires carrier approval first"],a:1,exp:"SetMaxBenefitAmount caps how much coverage an employee can elect. SetGILimit sets the Guaranteed Issue threshold."},
{q:"In Rule Set 1, what happens when the employee is in their new hire window but eligibilityDate falls before the cutOffDate?",opts:["They receive the full MAX_BEN_AMT","They receive LATE_ENTRANT_MAX_BEN_AMT — they were eligible before the cutoff and should have enrolled in the previous plan year","They are marked completely ineligible","They must complete underwriting regardless of amount"],a:1,exp:"Being eligible before the cutoff means the employee should have enrolled last plan year. They receive the late entrant max."},
{q:"What does GetStepDown() return?",opts:["The date when the step-down takes effect","The percentage of original coverage retained at the current age band","The specific age at which the next reduction occurs","The number of remaining benefit tier steps"],a:1,exp:"GetStepDown(benefitLevelIndex, ageBandIndex) returns the step-down percentage. Used in: adjustedIncrement = BASE_INCREMENT * (stepDown / 100)."},
{q:"Why does the AUL script set stepDown = 100 when GetStepDown() returns 0?",opts:["100 is the maximum allowed","0 from GetStepDown() means no step-down applies — but using 0 in the percentage formula would zero out all benefits","It flags the record for manual review","0 and 100 are equivalent sentinel values"],a:1,exp:"When GetStepDown() returns 0 it signals no step-down applies. The formula needs stepDown = 100 to produce BASE_INCREMENT * 1 = no reduction."}
],
12:[
{q:"How do you correctly check if a life event is a termination?",opts:["Event.IsTermination == true","Event.EventType == 'Terminated'","var theEvent = Event.EventType.toString(); if(theEvent == 'Terminated')","Event.Employee.Status == 'Terminated'"],a:2,exp:"Event.EventType is a .NET enum. Convert to string first: var theEvent = Event.EventType.toString(); then compare."},
{q:"Why should death terminations (ReasonTypeID 304) be excluded from standard termination date logic?",opts:["Death events don't fire OnLifeEvent","Death terminations have specific continuation rules that differ from standard termination","BenSelect handles death events separately automatically","ReasonTypeID 304 indicates COBRA continuation"],a:1,exp:"Death coverage typically continues until end of the month of death — the standard cutoff pattern is wrong for death events."},
{q:"Why does the Deduct Before Effective Date Override use two separate scripts?",opts:["BenSelect imposes a maximum script size limit","Part 1 runs on OnEligible to recalculate the effective date; Part 2 runs on OnLifeEvent to disable deduct-before — different event types","A single script can only be attached to one plan","Part 2 is optional"],a:1,exp:"Part 1 (OnEligible) corrects the effective date. Part 2 (OnLifeEvent) disables deduct-before. They fire at different times."},
{q:"What does the dayRange constant control in the Custom Term Date Override script?",opts:["The number of days in the grace period","The maximum number of days before or after the actual termination date a custom coverage end date is allowed to fall","The number of days in the current plan year","Days until the next effective date"],a:1,exp:"dayRange = 100 means the custom coverage termination date must fall within 100 days before or after the employment termination date."},
{q:"Where do you set the coverage termination date override in an OnLifeEvent script?",opts:["Event.Employee.TerminationDate","Event.Application.TerminationDate","Event.Action.TerminationDate","Event.Config.CoverageEndDate"],a:2,exp:"Event.Action.TerminationDate is set in OnLifeEvent to override when coverage ends."},
{q:"What is the single line of code in Part 2 of the Deduct Before override script?",opts:["Event.Application.EffectiveDate = effectiveDate;","Event.Config.DeductionDateBeforeEffectiveDate = 0;","Event.Action.DeductBefore = false;","Event.Engine.DisableDeductBefore();"],a:1,exp:"Part 2 contains: Event.Config.DeductionDateBeforeEffectiveDate = 0; This single line disables deduct-before for life event enrollments."}
],
13:[
{q:"In a report script, what does Event.Value represent?",opts:["The employee's total benefit value","The current column's data value in the current row being processed","The report's running grand total","The return value the script must output"],a:1,exp:"Event.Value holds the current column's data value. Read it to make decisions and write to it to change report output."},
{q:"How do you suppress an entire row from a BenSelect report?",opts:["return null","Set Event.Value = '' on every column","Set Event.SkipRecord = true","Set Event.Row.Visible = false"],a:2,exp:"Event.SkipRecord = true tells the report engine to skip the entire current row."},
{q:"What is Event.Globals used for in report scripting?",opts:["Storing hard-coded configuration constants","Persisting values across row processing iterations — e.g. running totals","Accessing company-level enrollment settings","Caching employee demographic data"],a:1,exp:"Event.Globals persists between rows during report execution. Initialize on Event.FirstRecord, accumulate per row."},
{q:"What is the functional difference between Event.FirstRecord and Event.FirstKeyRecord?",opts:["No difference","Event.FirstRecord is true only on the absolute first row; Event.FirstKeyRecord is true on the first row within each key group","Event.FirstKeyRecord is for CSV export; Event.FirstRecord for Excel","Event.FirstRecord applies to employee rows; Event.FirstKeyRecord to dependent rows"],a:1,exp:"Event.FirstRecord is true only for the absolute first row. Event.FirstKeyRecord is true for the first row within each key group."},
{q:"In a form or question script, what does Event.Value = false do?",opts:["Clears the value entered in the form field","Hides the form or question from the employee","Sets the numeric field value to zero","Marks the form as incomplete"],a:1,exp:"In forms and questions context, Event.Value = true shows the form/question; false hides it."},
{q:"Which of the following is a valid ReportEvent formatting method?",opts:["Event.Format.FormatSSN()","Event.FormatSSN()","Event.Record.FormatSSN()","Event.Value.FormatSSN()"],a:1,exp:"Event.FormatSSN() is called directly on the ReportEvent object. Other methods: Event.FormatPhone(), Event.FormatEIN()."}
],
14:[
{q:"What is the correct way to read a boolean custom field named 'Smoker'?",opts:["if(Event.Employee.CustomFields['Smoker'])","if(Event.Employee.CustomFields['Smoker'] == 1)","if(Event.Employee.GetCustom('Smoker') === true)","if(Event.Employee.Smoker == true)"],a:1,exp:"Boolean custom fields return 1 or 0. Always compare explicitly: == 1."},
{q:"Which EventType should a script use if it needs to adjust the effective date of coverage?",opts:["OnReport","OnLifeEvent","OnEligible","OnNewHire"],a:2,exp:"OnEligible is where you set Event.Application.EffectiveDate to override coverage start."},
{q:"What does Event.LastCoverage == null mean in an OnEligible script?",opts:["The employee has waived coverage","The employee has no prior coverage record for this product","The prior year coverage was cancelled","The plan year has not yet started"],a:1,exp:"null means the employee has never been enrolled in this product."},
{q:"How do you safely access the first plan year's start date when Event.PlanYear might be null?",opts:["Event.Case.FirstPlanYear.PlanYearStartDate","Event.Case.EnabledPlanYears[0].PlanYearStartDate","Event.PlanYear.Default.PlanYearStartDate","Event.Case.PlanYears.First().StartDate"],a:1,exp:"EnabledPlanYears[0] is the safe fallback — it always exists even in the first plan year."},
{q:"CoverageTypeID 4 in Selerix represents which coverage tier?",opts:["Employee + Spouse","Family (all dependents)","Employee Only","Employee + Children"],a:2,exp:"CoverageTypeID 4 = Employee Only tier."},
{q:"Which property suppresses an entire row in a BenSelect report script?",opts:["Event.HideRow = true","Event.SkipRecord = true","Event.Value = null","Event.Row.Skip()"],a:1,exp:"Event.SkipRecord = true suppresses the entire row from report output."}
],
}
